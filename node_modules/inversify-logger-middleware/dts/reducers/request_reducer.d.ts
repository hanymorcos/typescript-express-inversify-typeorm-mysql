import * as inversify from "inversify";
import interfaces from "../interfaces/interfaces";
declare function requestReducer(request: inversify.interfaces.Request, options: interfaces.RequestLoggerSettings): any;
export default requestReducer;
